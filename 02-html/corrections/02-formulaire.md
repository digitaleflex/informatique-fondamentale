# Correction — Formulaire

Vérifie la chaîne :

```text
label → id → name → type → contraintes → submit
```

`name` et `id` n'ont pas le même rôle. Le navigateur peut effectuer des validations natives, mais le serveur doit refaire les contrôles métier.