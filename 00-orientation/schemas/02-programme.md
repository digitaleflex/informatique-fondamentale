# Schéma — Du programme à l'exécution

Code source → interprétation/compilation → instructions exécutées → CPU + mémoire → résultat.

Un programme est une suite d'instructions transformées en opérations exécutables par la machine.