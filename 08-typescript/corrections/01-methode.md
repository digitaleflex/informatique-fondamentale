# Correction — TypeScript

Décomposer → implémenter le minimum → compiler/exécuter → tester → corriger → refactoriser.

La correction explique le raisonnement et les erreurs, pas seulement le résultat.