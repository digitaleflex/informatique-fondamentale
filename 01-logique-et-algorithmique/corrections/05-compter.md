# Correction — Compter

Une boucle répète la même opération avec une variable qui évolue.

Pour les nombres pairs, ajouter une condition sur le reste de la division par 2.

Leçon : la boucle répond à « combien de fois ? », la condition à « dans quel cas ? ».